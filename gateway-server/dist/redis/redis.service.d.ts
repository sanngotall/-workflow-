import { OnModuleInit, OnModuleDestroy } from '@nestjs/common';
import Redis from 'ioredis';
export declare class RedisService implements OnModuleInit, OnModuleDestroy {
    private client;
    private pubClient;
    private subClient;
    onModuleInit(): Promise<void>;
    onModuleDestroy(): Promise<void>;
    getClient(): Redis;
    get(key: string): Promise<string | null>;
    set(key: string, value: string | number, ttl?: number): Promise<void>;
    del(key: string): Promise<void>;
    exists(key: string): Promise<boolean>;
    hget(key: string, field: string): Promise<string | null>;
    hset(key: string, field: string, value: string): Promise<void>;
    hgetall(key: string): Promise<Record<string, string>>;
    incr(key: string): Promise<number>;
    expire(key: string, seconds: number): Promise<void>;
    publish(channel: string, message: string): Promise<void>;
    subscribe(channel: string, callback: (message: string) => void): Promise<void>;
    jsonSet(key: string, value: any): Promise<void>;
    jsonGet<T>(key: string): Promise<T | null>;
    getRouteKey(projectId: string, env: string, method: string, path: string): Promise<string>;
    getTransitRouteKey(transitId: string): Promise<string>;
    getRateLimitKey(routeId: string, clientIp: string): Promise<string>;
    getBreakerKey(routeId: string): Promise<string>;
    getBreakerWindowKey(routeId: string): Promise<string>;
    zadd(key: string, score: number, member: string): Promise<void>;
    zremrangebyscore(key: string, min: number, max: number): Promise<void>;
    zcard(key: string): Promise<number>;
    zcount(key: string, min: number, max: number): Promise<number>;
    zrem(key: string, member: string): Promise<void>;
    getBlacklistKey(jti: string): Promise<string>;
    setBlacklist(jti: string, ttlSec: number): Promise<void>;
    isBlacklisted(jti: string): Promise<boolean>;
    delBlacklist(jti: string): Promise<void>;
    getLoginFailKey(ip: string): Promise<string>;
    incrLoginFail(ip: string, windowSec: number): Promise<number>;
    resetLoginFail(ip: string): Promise<void>;
    getLoginFailCount(ip: string): Promise<number>;
}
